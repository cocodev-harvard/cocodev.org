---
export interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<li class="home-link">
  <a href={href}>
    <h2>{title}</h2>
    <p>{body}</p>
  </a>
</li>

<style>
  .home-link {
    @apply flex;
  }
</style>